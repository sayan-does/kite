[tool.poetry]
name = "test-poetry"
version = "0.1.0"

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.31.0"
click = ">=8.0"
numpy = {version = ">=1.24.0", extras = ["random"]}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
